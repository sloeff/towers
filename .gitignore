# Godot 4+ engine cache and imported resources
.godot/
/android/

# Exports - build output stays out, but the preset itself is tracked so CI
# can reproduce the web export (contains no signing keys or secrets)
/export/
/builds/

#Claude
.claude/
CLAUDE.md